<form method="post" action="{{ path('app_bibliographie_delete', {'id': bibliographie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bibliographie.id) }}">
    <button class="btn">Delete</button>
</form>
