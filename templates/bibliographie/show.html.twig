{% extends 'base.html.twig' %}

{% block title %}Bibliographie{% endblock %}

{% block body %}
    <h1>Bibliographie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bibliographie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bibliographie.name }}</td>
            </tr>
            <tr>
                <th>Url</th>
                <td>{{ bibliographie.url }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bibliographie_index') }}">back to list</a>

    <a href="{{ path('app_bibliographie_edit', {'id': bibliographie.id}) }}">edit</a>

    {{ include('bibliographie/_delete_form.html.twig') }}
{% endblock %}
